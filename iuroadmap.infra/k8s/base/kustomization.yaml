apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: iuroadmap

resources:
  - namespace.yaml
  - configmap.yaml
  - api-gateway.yaml
  - auth-service.yaml
  - admin-service.yaml
  - user-service.yaml
  - mentor-service.yaml
  - ingress.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: iuroadmap
